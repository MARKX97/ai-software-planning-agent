---
name: development
description: Claude entry for the shared development playbook.
triggers:
  - implementing a feature
  - writing code
  - code review
  - architecture drift
---

# Development Entry

先读取 `AGENTS.md`，再读取 `docs/playbooks/development.md` 和当前任务对应的 spec/contract。

通用开发规则不在本文件维护。
