/**
 * Shared package — types, enums, and Zod schemas.
 *
 * Phase 1 skeleton: types live in `src/types/`, enums in `src/enums/`,
 * schemas in `src/schemas/` per CLAUDE.md §A4.
 *
 * @internal
 */
export const SHARED_PACKAGE_NAME = '@ai-planning/shared';
