# Technical Debt And Harness Exceptions

> Status: Current
> Rule: 机器基线位于 `scripts/harness/code-shape-baseline.json`。现有指标只能下降，不能增加新豁免或扩大数值。

| ID           | 规则                  | 当前范围                                    | 影响                   | 移除条件                                   |
| ------------ | --------------------- | ------------------------------------------- | ---------------------- | ------------------------------------------ |
| HE-SHAPE-001 | 生产文件不超过 200 行 | `apps/web/src/types/api.ts`                 | API 类型导航成本偏高   | 按领域拆分类型后删除基线                   |
| HE-SHAPE-002 | 函数不超过 50 行      | 主要为现有 React 页面组件                   | 组件职责和测试定位偏大 | 业务改动触及对应组件时提取稳定子组件       |
| HE-SHAPE-003 | 参数不超过 3 个       | 部分 workflow helper、Controller 和 Adapter | 调用签名可读性较弱     | 改为命名参数对象或框架允许的封装后删除基线 |

这些是存量代码形态债务，不是架构、密钥、契约或 LLM 边界的豁免；后四类规则从启用起全量阻断。
